# Brew

Brew is a package manager for Linux/MacOS.

## Packages installed

List of all packages installed `brew list`:


``` 
abseil           crun            git     just          libksba       libusb     ncurses        podman       spdlog      zellij        │
act              curl            gitui   krb5          libmpc        libx11     nettle         protobuf     sqlite      zig           │
bat              dbus            glib    libassuan     libnghttp2    libxau     nlohmann-json  python@3.11  starship    zlib          │
bear             elfutils        gmp     libcap        libnsl        libxcb     npth           re2          systemd     zoxide        │
berkeley-db@5    expat           gnupg   libedit       libseccomp    libxcrypt  nushell        readline     unbound     zstd          │
binutils         fd              gnutls  libevent      libsecret     libxdmcp   openldap       rtmpdump     unzip                     │
brotli           fmt             gpgme   libffi        libslirp      libxml2    openssl@3      rust         util-linux                │
bzip2            fuse-overlayfs  grpc    libf
```
